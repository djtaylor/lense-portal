#!/bin/bash
# CloudScape Client Libraries: Post-removal Script

# Client exectuable
CLIENT_EXE="/usr/bin/cloudscape"

# Remove the client executable
[ -h $SUDOERS_DST ] && rm -f $SUDOERS_CONFIG

exit 0