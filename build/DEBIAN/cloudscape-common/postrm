#!/bin/bash
# CloudScape Common Libraries: Post-removal Script

# Profile configuration
PROFILE_CONFIG="/etc/profile.d/cloudscape.sh"

# Remove the old profile configuration
[ -f $PROFILE_CONFIG ] && rm -f $PROFILE_CONFIG

return 0